pyhive==0.6.5          # Hive bağlantısı için
thrift==0.16.0         # PyHive için gerekli bağımlılık
thrift-sasl==0.4.3     # HiveServer2 bağlantısı için gerekli
sasl==0.3.1            # Kimlik doğrulama için
requests==2.31.0       # API'den veri toplamak için
pandas==1.5.3          # Veri işleme ve analizi için
scikit-learn==1.2.2    # Anomali tespiti ve risk skorlama için
tensorflow==2.12.0     # Yapay zeka tabanlı analizler için (opsiyonel)
sqlalchemy==2.0.7      # Hive ve SQL entegrasyonu için (opsiyonel)